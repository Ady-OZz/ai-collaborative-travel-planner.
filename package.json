{
  "name": "ai-collaborative-travel-planner",
  "private": true,
  "scripts": {
    "start": "vercel dev",
    "deploy": "vercel",
    "build": "cd frontend && npm install && npm run build"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "@google/generative-ai": "^0.15.0",
    "cors": "^2.8.5"
  }
}
